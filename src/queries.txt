query Search {
  listGameInfos(limit: 5) {
    nextToken
    items {
      id
      shortDesc
      title
    }
  }
}

query CommentSection {
  commentsByGameID(gameID: "0") {
    items {
      id
      text
      accountID
      updatedAt
    }
  }
}
